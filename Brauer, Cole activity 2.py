# Write a program that gets 2 string variables and 2 number variables
# from the user, concatenates (joins them together with no spaces) and
# displays the strings, then multiplies the two numbers on a new line\
print("welcome to Cole's program!")
print("plese wait, loading...")
import time
time.sleep(2)
text_1 = input("type in a string:")
text_2 = input("type in another string:")
number_1 = float(input("type in a number:"))
number_2 = float(input("type in another number:"))
print("concatenates:", (text_1 + text_2))
print("Multiplies:", (number_1 * number_2))


